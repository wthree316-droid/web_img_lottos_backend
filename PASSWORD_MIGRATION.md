# 🔐 Password Migration Guide (Updated)

## ⚠️ สำคัญมาก! อ่านก่อนใช้งาน

หากคุณมี users ในระบบที่เก็บ password แบบ **plain text** อยู่แล้ว (จาก version เก่า) คุณ**ต้อง**รัน migration script ก่อน deploy production

## 🔧 การแก้ไขล่าสุด (v2.0)

**✅ รองรับ Password ยาวเกิน 72 bytes**

bcrypt มีข้อจำกัดที่ password ต้องไม่เกิน 72 bytes ดังนั้นเราได้ปรับปรุง migration script ให้:
- ถ้า password ≤ 72 bytes → hash ด้วย bcrypt โดยตรง
- ถ้า password > 72 bytes → hash ด้วย SHA256 ก่อน แล้วเอา hex digest (64 chars) ไป hash ด้วย bcrypt

วิธีนี้ปลอดภัยและทำให้ผู้ใช้สามารถใช้ password ยาวๆ ได้

---

## 📋 ขั้นตอนการ Migrate

### Step 1: Backup Database (แนะนำอย่างยิ่ง)

ก่อนทำอะไร ให้ backup ตาราง `users` ไว้ก่อน:

```sql
-- รันใน Supabase SQL Editor
CREATE TABLE users_backup AS SELECT * FROM users;
```

### Step 2: ติดตั้ง Dependencies ใหม่

```bash
cd backend
pip install -r requirements.txt
```

### Step 3: ตั้งค่า Environment Variables

ตรวจสอบว่าไฟล์ `.env` มีค่าถูกต้อง:

```bash
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key
```

### Step 4: รัน Migration Script

```bash
python migrate_passwords.py
```

คุณจะเห็น output คล้ายนี้:

```
==================================================
🔐 Password Migration Script
==================================================

⚠️  คำเตือน: สคริปต์นี้จะแก้ไข passwords ใน database
ต้องการดำเนินการต่อหรือไม่? (yes/no): yes

🔍 กำลังค้นหา users ที่มี plain text password...
✅ พบ 5 users
✅ admin: Password ถูก hash เรียบร้อย
⚠️  user01: Password ยาว 120 bytes (เกิน 72) - จะใช้ SHA256+bcrypt
✅ user01: Password ถูก hash เรียบร้อย
✅ user02: Password ถูก hash เรียบร้อย
⏭️  user03: Password ถูก hash แล้ว (ข้าม)
✅ user04: Password ถูก hash เรียบร้อย

==================================================
🎉 Migration เสร็จสิ้น!
   - อัพเดต: 4 users
   - ข้าม: 1 users (hash อยู่แล้ว)
==================================================
```

### Step 5: ทดสอบ Login

ทดสอบว่า login ยังใช้งานได้:

```bash
# ทดสอบด้วย curl
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"1234"}'
```

ถ้าได้ response พร้อม user data แสดงว่าสำเร็จ!

---

## 🔧 Technical Details: Password Hashing Strategy

### สำหรับ Password ≤ 72 bytes (ปกติ)
```
plain_password → bcrypt → $2b$12$...
```

### สำหรับ Password > 72 bytes (ยาว)
```
plain_password → SHA256 → hex_digest (64 chars) → bcrypt → $2b$12$...
```

**ทำไมใช้ SHA256?**
- bcrypt จำกัดที่ 72 bytes
- SHA256 hex digest = 64 characters = 64 bytes (น้อยกว่า 72)
- SHA256 เป็น one-way hash ที่ปลอดภัย
- ไม่สูญเสีย entropy ของ password เดิม

**ความปลอดภัย:**
- ✅ ป้องกัน rainbow table attacks
- ✅ ป้องกัน brute force attacks (bcrypt cost factor 12)
- ✅ รองรับ password ยาวไม่จำกัด
- ✅ Backward compatible (ระบบจะลอง verify ทั้ง 2 แบบ)

---

## 🚀 สำหรับ Production (Cloud Run)

### Option 1: รันผ่าน Cloud Shell

1. เปิด Cloud Shell ใน Google Cloud Console
2. Clone repo หรือ upload migration script
3. ตั้งค่า environment variables:
   ```bash
   export SUPABASE_URL="..."
   export SUPABASE_KEY="..."
   ```
4. รัน migration:
   ```bash
   python migrate_passwords.py
   ```

### Option 2: รัน Local แล้ว Point ไป Production Database

1. แก้ไข `.env` ให้ point ไปที่ production Supabase:
   ```bash
   SUPABASE_URL=https://production.supabase.co
   SUPABASE_KEY=production-service-role-key
   ```
2. รัน migration จาก local:
   ```bash
   python migrate_passwords.py
   ```

---

## ❓ FAQ

### Q: ถ้ารัน migration script หลายรอบจะเกิดอะไร?
**A:** ไม่มีปัญหา! Script จะเช็คว่า password ไหน hash แล้ว และจะข้ามไป (ไม่ hash ซ้ำ)

### Q: User จะต้องเปลี่ยน password ไหม?
**A:** **ไม่ต้อง!** User ใช้ password เดิมได้เหมือนเดิม (แค่เราเก็บในรูป hash แทน plain text)

### Q: ถ้า migration ล้มเหลวครึ่งทาง?
**A:** Script จะแสดง error แต่ continue ต่อไป รัน script อีกครั้งจะ hash เฉพาะตัวที่ยังไม่เสร็จ

### Q: จะรู้ได้ไงว่า password ถูก hash แล้ว?
**A:** Password ที่ hash แล้วจะขึ้นต้นด้วย `$2b$` หรือ `$2a$` ใน database

### Q: ถ้าเพิ่ม user ใหม่หลัง migrate?
**A:** ระบบจะ hash อัตโนมัติ (API endpoint `/api/users` แก้ไขแล้ว รองรับ password ยาว)

### Q: Password ยาวแค่ไหนถึงจะใช้ SHA256?
**A:** เกิน 72 bytes (ประมาณ 72 ตัวอักษร ASCII หรือ 24 ตัวอักษรไทย)

### Q: ระบบจะรู้ได้ไงว่าต้อง verify แบบไหน?
**A:** ระบบจะลอง verify ทั้ง 2 แบบ:
1. ลอง bcrypt โดยตรงก่อน (สำหรับ password ปกติ)
2. ถ้าไม่ผ่าน ลอง SHA256+bcrypt (สำหรับ password ยาว)

---

## 🔍 วิธีตรวจสอบว่า Migration สำเร็จ

### 1. เช็คใน Supabase SQL Editor

```sql
SELECT username, 
       LENGTH(password) as password_length,
       LEFT(password, 4) as password_prefix,
       CASE 
         WHEN password LIKE '$2b$%' OR password LIKE '$2a$%' 
         THEN '✅ Hashed' 
         ELSE '❌ Plain Text' 
       END as status
FROM users;
```

### 2. ทดสอบ Login API

ทุก user ควร login ได้ปกติด้วย password เดิม

---

## ⚠️ คำเตือน

1. **อย่า**ลืม backup database ก่อนรัน migration
2. **อย่า**รัน migration บน production โดยไม่ได้ทดสอบใน development ก่อน
3. **อย่า**แชร์ service role key ของ Supabase (ใช้เฉพาะ migration เท่านั้น)
4. **อย่า**ลบ `migrate_passwords.py` ออกจาก repo (เผื่อต้องใช้อีก)
5. **อย่า**ตกใจถ้าเห็นข้อความ "Password ยาง ... bytes" - เป็นเรื่องปกติ

---

## 🐛 Troubleshooting

### Error: "password cannot be longer than 72 bytes"
**แก้ไข:** ✅ แก้แล้วใน version 2.0! Script จะ hash ด้วย SHA256 ก่อนอัตโนมัติ

### Error: "module 'bcrypt' has no attribute '__about__'"
**แก้ไข:** อาจเป็นปัญหาของ bcrypt version - แต่ไม่กระทบการทำงาน (เป็นแค่ warning)

### Error: "Supabase credentials not found"
**แก้ไข:** ตรวจสอบว่าไฟล์ `.env` มีค่า `SUPABASE_URL` และ `SUPABASE_KEY`

### Error: "Connection timeout"
**แก้ไข:** ตรวจสอบ internet connection และ Supabase URL ถูกต้องหรือไม่

---

## 📝 หลัง Migrate เสร็จแล้ว

- ✅ ลบ `.env` ออกจากเครื่อง local (ถ้ามี production credentials)
- ✅ อัพเดท documentation ว่าระบบใช้ bcrypt + SHA256 แล้ว
- ✅ Commit โค้ดใหม่และ deploy
- ✅ ทดสอบ login ทุก user account

---

**Version:** 2.0 (Updated for long passwords)  
**Last Updated:** February 8, 2026  
**เสร็จแล้ว! ระบบของคุณปลอดภัยขึ้น 🔐**
